<%= form_for(@user) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name, class: "form-control form-control-sm" %>

	<%= f.submit, class: "btn btn-primary" do %>
		<%= icon('fas', 'check') %>
	<% end %>
<% end %>