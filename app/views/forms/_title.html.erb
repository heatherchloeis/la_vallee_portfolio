<%= form_for(@user) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, class: "form-control form-control-sm" %>

	<%= f.submit, class: "btn btn-primary" do %>
		<%= icon('fas', 'check') %>
	<% end %>
<% end %>