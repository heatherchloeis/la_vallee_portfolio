<%= form_for(@user) do |f| %>
	<%= f.label :bio %>
	<%= f.text_area :bio, class: "form-control form-control-sm", rows: "5" %>

	<%= f.submit, class: "btn btn-primary" do %>
		<%= icon('fas', 'check') %>
	<% end %>
<% end %>