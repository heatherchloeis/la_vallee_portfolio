<a class="resume-menu" data-toggle="dropdown" id="postMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
	<%= icon('fas', 'angle-down') %>
</a>
<div class="dropdown-menu" aria-labelledby="postMenuButton">
	<!-- Edit modal path -->
	<%= link_to "Edit experience", edit_experience_path(e), class: "dropdown-item", remote: true, data: { target: '#modal_container', toggle: 'modal' } %>
	<!-- Edit modal path -->
	<%= link_to "Delete experience", e, class: "dropdown-item", method: :delete, data: { confirm: "Are you sure?" } %>
</div>